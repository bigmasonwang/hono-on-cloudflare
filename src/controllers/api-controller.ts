import apiController from '@/factories/api-controller-factory'

// Re-export the default instance and types
export default apiController
export type { Contexts } from '@/factories/api-controller-factory'
